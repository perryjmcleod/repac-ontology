# GOVERNANCE L1 — Candidate Subsets

## Objective

Identify the controlled Governance Layer 1 candidate subset set for vNext core-extension while preserving strict separation between subset-layer and element-layer content.

## Authority

The authoritative Governance L1 subset inventory is maintained in:

- `ontology/vNext/core-extension/governance/diagrams/GOVERNANCE-L1-STRUCTURE.drawio`

## Current Structural State

Governance Layer 1 is not yet normalized into canonical `GOV-SUBSET-XX-name` markdown files.

The directory currently contains element-style artifacts using the `GOV-EL-*` pattern. These artifacts must not be treated as authoritative Layer 1 subset definitions without confirmation from the authoritative Governance diagram.

## Constraints

- No conceptual expansion
- No renaming without explicit semantic change control
- No ontology drift
- No element-level modelling at L1
- Mixed-layer Governance artifacts must not be treated as authoritative subset definitions

## Assessment

This file is restored as part of the structural repair pass.

Governance Layer 1 remains structurally incomplete until the authoritative subset inventory is transcribed from the governing diagram and normalized into canonical subset identifiers.
